
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user profile in the EvenTide application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "fullName": {
          "type": "string",
          "description": "User's full name."
        },
        "phoneNumber": {
          "type": "string",
          "description": "User's phone number."
        },
        "role": {
          "type": "string",
          "description": "User's role within the application (Owner, Planner, Guest, Security, Cohost)."
        }
      },
      "required": [
        "id",
        "email",
        "fullName",
        "phoneNumber",
        "role"
      ]
    },
    "Event": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Event",
      "type": "object",
      "description": "Represents an event created within the EvenTide application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Event entity."
        },
        "ownerId": {
          "type": "string",
          "description": "Reference to UserProfile representing the event owner. (Relationship: UserProfile 1:N Event)"
        },
        "eventCode": {
          "type": "string",
          "description": "Unique code generated for the event."
        },
        "eventType": {
          "type": "string",
          "description": "Type of event (Wedding, General Event, Birthday, etc.)."
        },
        "eventName": {
          "type": "string",
          "description": "Name of the event."
        },
        "eventDate": {
          "type": "string",
          "description": "Date and time of the event.",
          "format": "date-time"
        },
        "eventLocation": {
          "type": "string",
          "description": "Location of the event."
        },
        "primaryColor": {
          "type": "string",
          "description": "Primary color for the event's theme."
        },
        "secondaryColor": {
          "type": "string",
          "description": "Secondary color for the event's theme."
        },
        "isPublic": {
          "type": "boolean",
          "description": "Indicates whether the event is public or private."
        },
        "isTicketed": {
            "type": "boolean",
            "description": "Indicates if the event requires tickets for entry."
        }
      },
      "required": [
        "id",
        "ownerId",
        "eventCode",
        "eventType",
        "eventName",
        "eventDate",
        "eventLocation",
        "primaryColor",
        "secondaryColor",
        "isPublic",
        "isTicketed"
      ]
    },
    "Guest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Guest",
      "type": "object",
      "description": "Represents a guest invited to an event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Guest entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event the guest is invited to. (Relationship: Event 1:N Guest)"
        },
        "userProfileId": {
          "type": "string",
          "description": "Reference to the UserProfile of the guest. (Relationship: UserProfile 1:N Guest)"
        },
        "guestCode": {
          "type": "string",
          "description": "Unique code for the guest."
        },
        "guestCategory": {
          "type": "string",
          "description": "Category of the guest (Chairman, VIP, General)."
        },
        "rsvpStatus": {
          "type": "string",
          "description": "RSVP status of the guest (Accept, Decline, Maybe)."
        },
        "hasCheckedIn": {
          "type": "boolean",
          "description": "Indicates whether the guest has checked in to the event."
        }
      },
      "required": [
        "id",
        "eventId",
        "userProfileId",
        "guestCode",
        "guestCategory",
        "rsvpStatus",
        "hasCheckedIn"
      ]
    },
    "EventPlanner": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EventPlanner",
      "type": "object",
      "description": "Represents the relationship between an event and a planner.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the EventPlanner entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event being planned. (Relationship: Event 1:N EventPlanner)"
        },
        "plannerId": {
          "type": "string",
          "description": "Reference to the UserProfile of the planner. (Relationship: UserProfile 1:N EventPlanner)"
        }
      },
      "required": [
        "id",
        "eventId",
        "plannerId"
      ]
    },
    "EventCohost": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EventCohost",
      "type": "object",
      "description": "Represents a co-host assisting with an event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the EventCohost entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event. (Relationship: Event 1:N EventCohost)"
        },
        "cohostId": {
          "type": "string",
          "description": "Reference to the UserProfile of the cohost. (Relationship: UserProfile 1:N EventCohost)"
        }
      },
      "required": [
        "id",
        "eventId",
        "cohostId"
      ]
    },
    "ChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents a chat message within the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatMessage entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event the message belongs to. (Relationship: Event 1:N ChatMessage)"
        },
        "senderId": {
          "type": "string",
          "description": "Reference to the UserProfile who sent the message. (Relationship: UserProfile 1:N ChatMessage)"
        },
        "message": {
          "type": "string",
          "description": "Content of the chat message."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the message was sent.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "eventId",
        "senderId",
        "message",
        "timestamp"
      ]
    },
    "Media": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Media",
      "type": "object",
      "description": "Represents a media file (photo or video) associated with an event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Media entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event the media belongs to. (Relationship: Event 1:N Media)"
        },
        "uploaderId": {
          "type": "string",
          "description": "Reference to the UserProfile who uploaded the media. (Relationship: UserProfile 1:N Media)"
        },
        "fileUrl": {
          "type": "string",
          "description": "URL of the media file."
        },
        "uploadTimestamp": {
          "type": "string",
          "description": "Timestamp of when the media was uploaded.",
          "format": "date-time"
        },
        "mediaType": {
          "type": "string",
          "description": "Type of media (photo or video)."
        }
      },
      "required": [
        "id",
        "eventId",
        "uploaderId",
        "fileUrl",
        "uploadTimestamp",
        "mediaType"
      ]
    },
    "PricePlan": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PricePlan",
      "type": "object",
      "description": "Represents a pricing plan for events.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the PricePlan entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the price plan (Free, Standard, Gold, Platinum)."
        },
        "guestLimit": {
          "type": "number",
          "description": "Maximum number of guests allowed in the plan."
        },
        "securityPersonnelLimit": {
          "type": "number",
          "description": "Maximum number of security personnel allowed in the plan."
        },
        "plannerLimit": {
          "type": "number",
          "description": "Maximum number of planners allowed in the plan."
        },
        "cohostLimit": {
          "type": "number",
          "description": "Maximum number of cohosts allowed in the plan."
        },
        "price": {
          "type": "number",
          "description": "Cost of the price plan in Naira."
        }
      },
      "required": [
        "id",
        "name",
        "guestLimit",
        "securityPersonnelLimit",
        "plannerLimit",
        "cohostLimit",
        "price"
      ]
    },
    "EventPricing": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EventPricing",
      "type": "object",
      "description": "Represents the price plan assigned to a specific event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the EventPricing entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event. (Relationship: Event 1:1 EventPricing)"
        },
        "pricePlanId": {
          "type": "string",
          "description": "Reference to the PricePlan. (Relationship: PricePlan 1:1 EventPricing)"
        }
      },
      "required": [
        "id",
        "eventId",
        "pricePlanId"
      ]
    },
    "SecurityCode": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SecurityCode",
      "type": "object",
      "description": "Stores the security codes assigned for the security role in an event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SecurityCode entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event this security code is for. (Relationship: Event 1:N SecurityCode)"
        },
        "securityId": {
          "type": "string",
          "description": "Reference to the UserProfile representing the security personnel. (Relationship: UserProfile 1:N SecurityCode)"
        },
        "securityCode": {
          "type": "string",
          "description": "The unique security code for the personnel."
        }
      },
      "required": [
        "id",
        "eventId",
        "securityId",
        "securityCode"
      ]
    },
    "Announcement": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Announcement",
      "type": "object",
      "description": "Represents announcements or notifications relating to an event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Announcement entity."
        },
        "eventId": {
          "type": "string",
          "description": "Reference to the Event the announcement belongs to. (Relationship: Event 1:N Announcement)"
        },
        "authorId": {
          "type": "string",
          "description": "Reference to the UserProfile who made the announcement. (Relationship: UserProfile 1:N Announcement)"
        },
        "content": {
          "type": "string",
          "description": "Content of the announcement."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp of when the announcement was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "eventId",
        "authorId",
        "content",
        "timestamp"
      ]
    },
    "Ticketier": {
        "title": "Ticketier",
        "type": "object",
        "description": "Represents a Ticketier or Promoter's public profile.",
        "properties": {
            "id": { "type": "string", "description": "The user's UID." },
            "promoterName": { "type": "string", "description": "The public name of the promoter or brand." },
            "bio": { "type": "string", "description": "A short biography for the promoter's public page." },
            "avatarUrl": { "type": "string", "description": "URL for the promoter's avatar or logo." }
        },
        "required": ["id", "promoterName"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information. Path-based ownership ensures only the user can access their own profile.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user. Corresponds to the Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/ticketiers/{ticketierId}",
        "definition": {
          "entityName": "Ticketier",
          "schema": {
            "$ref": "#/backend/entities/Ticketier"
          },
          "description": "Stores public profiles for Ticketiers/Promoters.",
          "params": [
            {
              "name": "ticketierId",
              "description": "The unique identifier of the ticketier, corresponding to their UID."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}",
        "definition": {
          "entityName": "Event",
          "schema": {
            "$ref": "#/backend/entities/Event"
          },
          "description": "Stores event details. Includes the ownerId to identify the event creator.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/guests/{guestId}",
        "definition": {
          "entityName": "Guest",
          "schema": {
            "$ref": "#/backend/entities/Guest"
          },
          "description": "Stores guest information related to an event.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "guestId",
              "description": "The unique identifier of the guest."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/planners/{plannerId}",
        "definition": {
          "entityName": "EventPlanner",
          "schema": {
            "$ref": "#/backend/entities/EventPlanner"
          },
          "description": "Stores event planner assignments for an event. Acts as a membership map indicating who is an event planner.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "plannerId",
              "description": "The unique identifier of the planner."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/cohosts/{cohostId}",
        "definition": {
          "entityName": "EventCohost",
          "schema": {
            "$ref": "#/backend/entities/EventCohost"
          },
          "description": "Stores event cohost assignments for an event. Acts as a membership map indicating who is a cohost.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "cohostId",
              "description": "The unique identifier of the cohost."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/chat_messages/{messageId}",
        "definition": {
          "entityName": "ChatMessage",
          "schema": {
            "$ref": "#/backend/entities/ChatMessage"
          },
          "description": "Stores chat messages for an event.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "messageId",
              "description": "The unique identifier of the chat message."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/media/{mediaId}",
        "definition": {
          "entityName": "Media",
          "schema": {
            "$ref": "#/backend/entities/Media"
          },
          "description": "Stores media files (photos/videos) associated with an event.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "mediaId",
              "description": "The unique identifier of the media."
            }
          ]
        }
      },
      {
        "path": "/price_plans/{pricePlanId}",
        "definition": {
          "entityName": "PricePlan",
          "schema": {
            "$ref": "#/backend/entities/PricePlan"
          },
          "description": "Stores the details of available pricing plans.",
          "params": [
            {
              "name": "pricePlanId",
              "description": "The unique identifier of the price plan."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/event_pricing",
        "definition": {
          "entityName": "EventPricing",
          "schema": {
            "$ref": "#/backend/entities/EventPricing"
          },
          "description": "Stores the pricing plan assigned to an event.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/security_codes/{securityCodeId}",
        "definition": {
          "entityName": "SecurityCode",
          "schema": {
            "$ref": "#/backend/entities/SecurityCode"
          },
          "description": "Stores security codes for security personnel assigned to an event.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "securityCodeId",
              "description": "The unique identifier of the security code."
            }
          ]
        }
      },
      {
        "path": "/events/{eventId}/announcements/{announcementId}",
        "definition": {
          "entityName": "Announcement",
          "schema": {
            "$ref": "#/backend/entities/Announcement"
          },
          "description": "Stores announcements related to an event.",
          "params": [
            {
              "name": "eventId",
              "description": "The unique identifier of the event."
            },
            {
              "name": "announcementId",
              "description": "The unique identifier of the announcement."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure is designed to ensure security, scalability, and ease of debugging, following the principles of Authorization Independence, Clarity of Intent, DBAC, and QAPs. Authorization independence is achieved through denormalization of authorization data, specifically, the `members` map in events and the replication of relevant event data in subcollections. This eliminates the need for costly and insecure `get()` calls in security rules. Structural Segregation is implemented by keeping data with different access requirements in separate collections (e.g., user profiles are separate from event data). The structure follows consistent access modeling patterns, using path-based ownership for user-owned data and membership maps for collaborative data. This approach directly facilitates QAPs (Rules are not Filters) because list operations can be secured without filtering data based on client-provided values, as the necessary authorization data is readily available in the document being listed. The structure uses explicit state modeling, predictable schemas, and radical consistency in naming conventions for improved clarity and maintainability. \n\nSpecifically, Event access control is managed by the `Event.ownerId` and denormalized maps in collections like EventPlanner, EventCohost and Guest ensuring authorization rules can be applied without needing to read other documents. The `isPublic` field enables secure list operations for public events. The `UserProfile` documents are kept separate and secured via path-based access control.\n\nThe Membership Map pattern is applied throughout the design wherever collaborative access is required (e.g. event planners and cohosts).  The Guests are structured under the Events collection to simplify data querying and management."
  }
}
