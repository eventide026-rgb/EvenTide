
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function EventPollsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Event Polls</CardTitle>
        <CardDescription>Participate in live polls and see the results in real-time.</CardDescription>
      </CardHeader>
    </Card>
  );
}
