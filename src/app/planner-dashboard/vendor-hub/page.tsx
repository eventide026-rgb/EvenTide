
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function VendorHubPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Vendor Hub</CardTitle>
        <CardDescription>Discover, vet, and send contract proposals to vendors.</CardDescription>
      </CardHeader>
    </Card>
  );
}
