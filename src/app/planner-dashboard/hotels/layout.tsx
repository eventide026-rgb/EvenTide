
'use client';
// This can be a simple layout that doesn't add any extra UI if not needed
export default function HotelSearchLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
