
import { Card, CardDescription, CardHeader, CardTitle, CardContent } from '@/components/ui/card';

export default function DropdownsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Dropdown Menu Management</CardTitle>
        <CardDescription>Manage the links that appear in the main site header's navigation menus.</CardDescription>
      </CardHeader>
      <CardContent>
         <p className="text-muted-foreground text-center py-16">Dropdown menu management interface will be here.</p>
      </CardContent>
    </Card>
  );
}
