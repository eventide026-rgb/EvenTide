
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ContractsTasksPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Contracts & Tasks</CardTitle>
        <CardDescription>Oversee all vendor contracts and track the progress of planning tasks.</CardDescription>
      </CardHeader>
    </Card>
  );
}
