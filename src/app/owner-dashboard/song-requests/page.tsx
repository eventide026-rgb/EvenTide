
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function SongRequestsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Song Requests</CardTitle>
        <CardDescription>Allow guests to request songs for your event.</CardDescription>
      </CardHeader>
    </Card>
  );
}
