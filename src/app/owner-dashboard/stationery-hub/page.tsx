
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function StationeryHubPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Stationery Hub</CardTitle>
        <CardDescription>Design your master theme for invitations, gate passes, and more.</CardDescription>
      </CardHeader>
    </Card>
  );
}
