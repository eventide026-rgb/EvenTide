
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function FindPlannerPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Find a Planner</CardTitle>
        <CardDescription>Discover and hire professional event planners from the EvenTide network.</CardDescription>
      </CardHeader>
    </Card>
  );
}
