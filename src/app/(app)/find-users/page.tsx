import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function FindUsersPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Find Users</CardTitle>
        <CardDescription>Search for other users (like planners) by email to connect with them and add them to your team.</CardDescription>
      </CardHeader>
    </Card>
  );
}
