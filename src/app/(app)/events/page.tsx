import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function EventsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Events</CardTitle>
        <CardDescription>Create new events and manage all your existing ones from a central location.</CardDescription>
      </CardHeader>
    </Card>
  );
}
