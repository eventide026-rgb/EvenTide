
import { Card, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function ProgramViewerPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Program Viewer</CardTitle>
        <CardDescription>Access the event's schedule and your specific cues.</CardDescription>
      </CardHeader>
    </Card>
  );
}
